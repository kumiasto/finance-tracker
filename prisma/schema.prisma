generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id Int @default(uuid())
  name String
  email String @unique
}

model Income {
  id Int @default(uuid())

}

model Expenses {
  id Int @default(uuid())

}